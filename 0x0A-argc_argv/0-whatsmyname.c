#include <stdio.h>

/**
 * main - PRINTS ITS NAME
 * @argc: ardument count
 * @argv: array of pointers to argument strings
 * Return: 0
 */

int main(int argc, char *argv[])
{
	argc = argc;
	printf("%s\n", *argv);
	return (0);
}
